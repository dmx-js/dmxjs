import {Manufacturer} from '../manufacturers.ts';
import {fixtureMetadata} from '../utils.ts';

export const MiniKintaILS4Channel = fixtureMetadata({
	name: 'Mini Kinta ILS (4 channel)',
	manufacturer: Manufacturer.CHAUVET_DJ,
	channels: 4,
});
